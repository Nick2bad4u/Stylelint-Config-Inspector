// Legacy alias kept for compatibility with tools/users that reference eslint.config.strict.
// The project standard config is now eslint.config.js (Nuxt + Antfu).
import config from "./eslint.config.js";

export default config;